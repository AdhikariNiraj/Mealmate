name: mealmateapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.0
#  firebase_auth: ^4.15.0
  firebase_database: ^10.3.7
  firebase_storage: ^11.5.5
  provider: ^6.1.1
  google_fonts: ^6.2.1  # Updated version
  share_plus: ^7.2.1
  sensors_plus: ^4.0.2
  image_picker: ^0.8.7+5
  http: ^1.3.0
  cloud_firestore: ^4.13.0
  firebase_auth: ^5.5.2
  firebase_app_check: ^0.3.2+5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true